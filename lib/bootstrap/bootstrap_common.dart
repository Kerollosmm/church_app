/// Common bootstrap configuration shared by all app variants.
///
/// Responsibilities:
/// - Initialize Firebase (Auth, Firestore).
/// - Configure GetIt dependency injection.
/// - Setup logging and error reporting.
/// - Initialize common services (NetworkInfo, Logger).
