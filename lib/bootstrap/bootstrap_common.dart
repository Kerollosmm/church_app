/// Shared bootstrap logic for all applications (Servant and Admin).
///
/// Responsibilities:
/// - Initialize Flutter binding.
/// - Initialize Firebase (Auth, Firestore).
/// - Initialize core Dependency Injection (GetIt).
/// - Initialize Hive (local storage).
/// - Initialize global crash reporting / logging.
/// - Initialize common services (NetworkInfo, Logger).
