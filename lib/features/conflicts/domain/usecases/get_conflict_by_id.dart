/// Use case to fetch a single conflict case by ID.
///
/// Responsibilities:
/// - Call conflict repository with conflictId.
/// - Return ConflictCase entity with all version data.
/// - Used when admin opens the side-by-side comparison view.
